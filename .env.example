# Local MongoDB, as installed by the setup script. Point this at a remote
# server (mongodb://user:password@host:27017/) to use one instead.
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB=random_server

RUST_LOG=info

HOST=127.0.0.1
PORT=8080
